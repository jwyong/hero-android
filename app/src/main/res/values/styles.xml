<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryBG</item>
        <item name="colorPrimaryDark">@color/secondaryBG</item>
        <item name="colorAccent">@color/blueAccent1</item>
        <item name="android:fontFamily">@font/aktiv_grotesk_w01_regular</item>
    </style>

    <!--no action bar-->
    <style name="AppThemeNoActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryBG</item>
        <item name="colorPrimaryDark">@color/secondaryBG</item>
        <item name="colorAccent">@color/blueAccent1</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@color/primaryBG</item>
        <item name="android:fontFamily">@font/aktiv_grotesk_w01_regular</item>
    </style>

    <!--Toolbar-->
    <style name="toolbarBackBtnWhite" parent="Widget.AppCompat.Light.ActionBar">
        <!--<item name="android:homeAsUpIndicator">@drawable/ic_toolbar_back_button_white</item>-->
    </style>

    <!--bottom sheet-->
    <style name="btmSheetBgTrans" parent="Widget.AppCompat.Light.ActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:colorBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.3</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:animation">@anim/nav_default_enter_anim</item>
    </style>
</resources>
